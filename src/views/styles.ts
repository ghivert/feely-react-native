import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  full: {
    flex: 1,
  },
  row: {
    flexDirection: 'row',
  },
  baseline: {
    alignItems: 'baseline',
  },
  responsiveSquare: {
    flex: 1,
    aspectRatio: 1,
  },
})

export default styles
